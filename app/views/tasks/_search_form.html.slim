.well ng-controller='TaskFilterController'
  =simple_form_for(@search, url: tasks_path, method: 'GET', as: :search) do |f|
    .row
      .col-lg-2
        =f.input :from, label: false, placeholder: t('task.search.from'), as: :date_picker, input_html: { :'ng-model' => 'from', datepicker: true, :'ng-initial' => true }
      .col-lg-2
        =f.input :to, label: false, placeholder: t('task.search.to'), as: :date_picker, input_html: { :'ng-model' => 'to', datepicker: true, :'ng-initial' => true }
      .col-lg-4
        =f.select :projects, @projects, {}, multiple: true, selectize2: true
      .col-lg-2
        =f.select :payable, payable_options, {}, selectize2: true
      .col-lg-2
        =f.submit t('common.buttons.filter'), class: 'btn btn-default'

    .btn-toolbar
      .btn-group
        button.btn.btn-default.btn-xs type='button' ng-click='today()' =t('task.search.today')
        button.btn.btn-default.btn-xs type='button' ng-click='yesterday()' =t('task.search.yesterday')
      .btn-group
        button.btn.btn-default.btn-xs type='button' ng-click='thisIteration()' =t('task.search.this_iteration')
        button.btn.btn-default.btn-xs type='button' ng-click='previousIteration()' =t('task.search.previous_iteration')
      .btn-group
        button.btn.btn-default.btn-xs type='button' ng-click='thisWeek()' =t('task.search.this_week')
        button.btn.btn-default.btn-xs type='button' ng-click='previousWeek()' =t('task.search.previous_week')
      .btn-group
        button.btn.btn-default.btn-xs type='button' ng-click='thisMonth()' =t('task.search.this_month')
        button.btn.btn-default.btn-xs type='button' ng-click='previousMonth()' =t('task.search.previous_month')
