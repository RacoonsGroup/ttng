en:
  date:
    formats:
      default: '%d.%m.%Y'
  datetime:
    formats:
      default: '%d.%m.%Y'
  time:
    formats:
      default: '%H:%M %d.%m.%Y'
  helpers:
    submit:
      create: "Create %{model}"
      update: "Update %{model}"
  activerecord:
    errors:
      models:
        project_user:
          attributes:
            user:
              user_position: cannot have a position nobody
